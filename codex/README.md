# codex

Codex 资产总目录。  
这一层的作用是统一承载和组织 Codex 相关能力，而不是放业务代码。

## 目录职责

- `skills/`：可复用 Skills 清单与入口说明。

## 使用方式

1. 先进入 `skills/` 查看可用技能与适用场景。
2. 选择目标 skill 后，优先阅读该 skill 的 `SKILL.md`。
3. 按 `SKILL.md` 的流程执行，并结合实际仓库最小化调整。

## 设计约定

- 每个 skill 只做一类明确任务，避免“大而全”。
- skill 文档、脚本、参考资料保持同目录聚合，方便迁移和复用。
